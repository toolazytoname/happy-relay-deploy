diff --git a/sources/app/api/api.ts b/sources/app/api/api.ts
index 7c87ebf..d338604 100644
--- a/sources/app/api/api.ts
+++ b/sources/app/api/api.ts
@@ -292,7 +292,8 @@ export async function startApi() {
 
     // Start HTTP 
     const port = process.env.PORT ? parseInt(process.env.PORT, 10) : 3005;
-    await app.listen({ port, host: '0.0.0.0' });
+    const host = process.env.HAPPY_BIND_HOST?.trim() || '127.0.0.1';
+    await app.listen({ port, host });
     onShutdown('api', async () => {
         await app.close();
     });
